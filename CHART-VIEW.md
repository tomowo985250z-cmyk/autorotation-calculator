# チャート画像とドットの設定

`autorotation-chart.png` を開いて軸ラベルとグリッドを確認し、画像の暗い直線の中心を測定して `chart-view-config.js` に登録しました。原典確認前の暫定校正です。

| 項目 | 設定値 |
|---|---|
| 画像 | `./autorotation-chart.png` |
| 実ピクセルサイズ | 750 × 1334 |
| left / right | 90.5 / 713.5 px |
| top / bottom | 125 / 1210.5 px |
| 横軸 | 1700〜2600 lb |
| 縦軸 | −3000〜5000 ft |

左端1700 lbは、1800・2000などの数値ラベルと100 lbごとの縦グリッドを対応付けた値です。文字・数字・余白は座標領域に含めていません。線幅は約1〜3 pxあり、中心位置の読み取り精度は約±1.5 pxです。

## 登録する設定

- `image.src`：リポジトリ内の実画像への相対パス
- `image.width / height`：その元画像のピクセル幅・高さ
- `plot.left / right / top / bottom`：元画像左上を原点とした、実際のプロット領域の端のピクセル座標
- `axes.grossWeightLb.left / right`：プロット左端・右端の総重量（lb）
- `axes.densityAltitudeFt.top / bottom`：プロット上端・下端の密度高度（ft）

`axes.*.ticks` に実測した各目盛の `{ value, pixel }` を登録しています。Xは100 lb、Yは500 ftごとです。画像上の間隔がわずかに不均等なため、端点だけの比例変換では例えば2200 lbの縦線から約0.9 px、1800 lbでは約5.2 pxずれます。各目盛間を個別に線形補間することでグリッド位置に合わせます。画像の回転や曲がった線に対する2次元変形補正ではありません。

## 座標変換

入力は丸め前の `result.point` のみです。RPM算出可否・MINIMUM/MAXIMUM警告とは独立しています。

目盛校正がある場合、各軸の値を挟む2本の目盛線を探し、`pixel = pixel1 + (value − value1) / (value2 − value1) × (pixel2 − pixel1)` で補間してから画像全体に対する百分率にします。以下の端点による式は、目盛校正のない画像設定用のフォールバックです。

```text
x比率 = (grossWeightLb − 左端重量) / (右端重量 − 左端重量)
y比率 = (上端高度 − densityAltitudeFt) / (上端高度 − 下端高度)
left% = (plot.left + x比率 × (plot.right − plot.left)) / image.width × 100
top%  = (plot.top  + y比率 × (plot.bottom − plot.top)) / image.height × 100
```

画像と1個の赤いドットを同じ `position: relative` コンテナに置き、ドットは `position: absolute` と百分率で配置します。画像は `width: 100%; height: auto` で、切り抜きや余白を加えません。ドットの中心を `translate(-50%, -50%)` で座標に合わせます。縮小時も画像と同じ割合で位置が変わります。

軸範囲外は「チャート表示範囲外」とし、ドットを非表示にします。端への固定はしません。設定不足・画像読込失敗・元画像サイズ不一致もドットを非表示にします。画像だけ登録した場合は画像を表示し、位置合わせ未設定と案内します。

## 検証

`node tests/chart-view.test.js` で仮想画像による一般動作に加え、実画像の代表的な6交点、目盛間補間、境界超過時の表示、画像範囲外を検証します。

代表交点の実測中心と変換結果：1800 lb / 4000 ft → (154.5, 260)、2000 lb / 3000 ft → (291.5, 396.5)、2200 lb / 0 ft → (437.5, 803)、2400 lb / −1000 ft → (576.5, 939.5) px。元画像上の該当する暗い線との一致も確認しました。百分率で位置を保持するため画像縮小時も同じ割合で追従します。DOMテストはブラウザーの実描画そのものの検証ではありません。
