# RPM線の画像座標校正

`autorotation-chart.png`（750 × 1334 px）を目視確認し、グリッド・文字と区別できるRPM線の暗いピクセル中心を読み取りました。`rpm-image-data.js` に13線・計171点を登録しています。線のラベル、縦グリッドとの重なり、破線の空白は避け、上部と下部の複数高度で確認しました。各線は11〜15点程度で、点数と正確な座標は同ファイルを参照してください。

校正値は「チャート画像からの暫定デジタイズ値・原典確認前」です。線中心の読取精度は約1〜2 pxです。校正点の間（文字や破線で隠れている部分を含む）は線形補間によるモデルであり、全ピクセルを直接読み取った値ではありません。RPM精度を飛行資料の精度として保証するものではありません。

## 計算方法

1. 既存の `chart-view-config.js` と `AutorotationChartView.project()` をそのまま使用し、丸め前の `result.point` をドットと同一の画像座標へ変換します。
2. 各RPM線について、ドットのyを挟む2つの読取点を使い、その高さのxを補間します。共通の傾きや平行線は仮定しません。
3. ドットを左右に挟む隣接RPM線を求めます。
4. `fraction = (dot.x − left.x) / (right.x − left.x)`、`RPM = left.RPM + fraction × (right.RPM − left.RPM)` とします。
5. ±5 RPMは境界警告と無関係に算出します。

332 MINIMUMは最低の実測RPM参照線として332〜335間の補間にも使用します。境界管理では独立したminimumとして参照します。385 MAXIMUMは警告専用であり、補間系列には入れず380〜390間を連続補間します。そのため、385破線上でも補間RPMが厳密に385になるとは限りません。

線の読取点のy範囲を超える延長、隣接線が欠けた区間の飛び越し、チャート外への外挿はしません。332より左側には別のRPM参照線がないため算出しません。画像グリッドの最上端・最下端や線が画像の外へ出る部分も、読取点が足りなければRPM未算出となります。ドット位置と参考警告は保持します。警告境界も確認点のy範囲外では「境界判定未確定」です。

`result.imagePoint` は使用した画像座標、`result.interpolation` は左右RPM・x座標・補間率です。物理座標は従来どおり `result.point` にそのまま保持します。密度高度・総重量の計算式とドットの校正値は変更していません。

## 2,200 lb / 密度高度0 ft

| 項目 | 値 |
|---|---|
| ドット (x, y) | (437.5, 803) px |
| 左RPM線 | 355 RPM、x = 427.75 px |
| 右RPM線 | 360 RPM、x = 461.4 px |
| 補間率 | 0.2897473997 |
| 基準RPM | 356.4487369985 |
| ±5 RPM | 351.4487369985〜361.4487369985 |

355線は読取点(427, 800)と(429.5, 810)、360線は(460.5, 800)と(463.5, 810)を使用しています。画像の0 ftグリッドそのものが線を覆うため、その上下で確認した値からy=803に補間しています。

## 独立確認

校正に使わなかったy=200, 360, 860, 1080 pxの41点で照合し、xの最大差は0.8334 px未満でした。`tests/chart-data.test.js` に読取値と許容差1.5 pxを保存しています。全校正点と独立確認点が元画像の暗いピクセル上にあることは `tests/rpm-image-pixels.ps1` でも確認できます。

| 総重量 / 密度高度 | ドット (x, y) px | 左右RPM | 左右x px | 基準RPM |
|---|---|---|---|---|
| 2,000 lb / 3,000 ft | (291.5, 396.5) | 350 / 355 | 279.5909 / 314.4292 | 351.7092 |
| 2,400 lb / −1,000 ft | (576.5, 939.5) | 370 / 375 | 566.2857 / 600.7857 | 371.4803 |
| 2,507.75 lb / 0 ft | (651.6925, 803) | 380 / 390 | 594.9 / 670.9 | 387.4727 |

最後の例は385 MAXIMUM破線（x=630.05）より右側で、警告を表示しながらRPM・±5 RPM・ドットを保持します。
